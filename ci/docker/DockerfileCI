FROM cf1dck.cfavorita.net/jdk/kruger-jdk-alpine:1.0.0

ENV TZ ${TZ}

MAINTAINER components ""

# Default to UTF-8 file.encoding
ENV LANG C.UTF-8

EXPOSE 8080

WORKDIR /api

COPY kodex-root-services/build/libs/*.jar /api/app.jar

RUN ls /api/

ENTRYPOINT exec java -jar app.jar
